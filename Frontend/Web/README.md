# BFF.Web

Frontend sencillo con un menu lateral fijo para navegar entre vistas de items y una vista para episodios, incluye buscador por id, paginador y vista de detalle al seleccionar un registro.

## Levantar el server del client:

Corriendo el comando `ng serve` la app web se desplegará en:
```
http://localhost:4200
```

